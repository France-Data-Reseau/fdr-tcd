# =============================================================================
# Configuration FNCCR V2 — copier en `.env` et remplir. JAMAIS commité.
# Sur le VPS : chmod 600. Voir docs_architecture/05_DEPLOIEMENT.md.
# =============================================================================

# --- Application ---
# production => exige SECRET_KEY + SMTP complets, cookies Secure, HSTS
ENVIRONMENT=development
# Générer : python -c "import secrets; print(secrets.token_urlsafe(32))"
# NOUVELLE clé pour la V2 (ne pas réutiliser celle de la v1)
SECRET_KEY=
# URL publique — sert à construire les liens des emails (jamais le header Host)
APP_PUBLIC_URL=http://localhost:8000

# --- Grist (source de données unique) ---
# Sur le VPS : reprendre les valeurs de ~/stack/app_form_fnccr/.env
GRIST_API_KEY=
GRIST_DOC_ID=
GRIST_SERVER_URL=https://grist.francedatareseau.fr

# --- SMTP (VITAL en production : magic links + notifications admin) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
# Optionnel (défaut = SMTP_USER)
SMTP_FROM=
# Destinataires des notifications d'inscription, séparés par des virgules
ADMIN_NOTIFY_EMAILS=

# --- SSO OIDC : « connexion avec son compte Grist/France Data Réseau » ---
# Le SSO s'active automatiquement quand les 3 variables sont renseignées
# (bouton sur la page de login). Valeurs à obtenir de l'opérateur de
# grist.francedatareseau.fr : URL de l'issuer OIDC + enregistrement client
# avec redirect URI https://fdr2.revorun.eu/auth/callback
# Tant que c'est vide : connexion par magic link uniquement.
OIDC_ISSUER=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
